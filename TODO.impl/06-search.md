# 06 — Search

- [ ] Omnibox ⌘K/Ctrl-K/"/" island — src/lib/omnibox-filter.ts pure seam + spec; tag/name instant lookup via /data/index.json
- [ ] Pagefind post-build full-text (/search/ page); graceful in dev

**Done when:** omnibox spec green; pagefind index builds in CI.

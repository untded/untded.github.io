# 04 — Directory index + filter island

- [x] /elements/ — SSR <table> of all rows (no-JS baseline) + DirectoryFilter island (client:visible)
- [x] src/lib/directory-filter.ts — pure filter seam (text/category/status/change/charset) + spec
- [x] URL param sync (?q= &cat= &status= &change= &cs=), result count, shared filter.css

**Done when:** filters correct in spec; SSR table count = 1504; no-JS degrades to full table.

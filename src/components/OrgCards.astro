---
import { href } from '../lib/site'
import { ISO_TC154_URL, UNECE_UNCEFACT_URL } from '../lib/site'

interface Org {
  name: string
  sub: string
  role: string
  logo: { light: string; dark: string; alt: string; class: string }
  url: string
}

const orgs: Org[] = [
  {
    name: 'UNECE — UN/CEFACT',
    sub: 'United Nations Economic Commission for Europe, Centre for Trade Facilitation and Electronic Business',
    role: 'The United Nations body maintaining this directory (ISO 7372 / TDED), jointly with ISO/TC 154 through JWG 9.',
    logo: { light: '/img/logo-unece-light.svg', dark: '/img/logo-unece-dark.svg', alt: 'UNECE logo', class: 'h-14' },
    url: UNECE_UNCEFACT_URL,
  },
  {
    name: 'ISO/TC 154',
    sub: 'Processes, data elements and documents in commerce, industry and administration',
    role: 'The ISO committee that publishes ISO 7372 in parallel with the UN; its plenary resolutions mandate this open-source platform.',
    logo: { light: '/img/logo-iso-light.svg', dark: '/img/logo-iso-dark.svg', alt: 'ISO logo', class: 'h-14' },
    url: ISO_TC154_URL,
  },
]
---

<div class="grid gap-4 sm:grid-cols-2">
  {
    orgs.map((org) => (
      <a
        href={org.url}
        class="flex flex-col rounded-md border border-line bg-paper p-5 transition-colors hover:border-un/60"
      >
        <img src={href(org.logo.light)} alt={org.logo.alt} class={`${org.logo.class} w-auto dark:hidden`} width="auto" height="auto" />
        <img src={href(org.logo.dark)} alt="" aria-hidden="true" class={`hidden ${org.logo.class} w-auto dark:block`} width="auto" height="auto" />
        <h3 class="mt-4 text-base font-semibold text-ink">{org.name}</h3>
        <p class="mt-1 text-xs uppercase tracking-wide text-body/80">{org.sub}</p>
        <p class="mt-3 text-sm leading-relaxed text-body">{org.role}</p>
      </a>
    ))
  }
</div>

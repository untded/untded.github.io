# 25 — Printable reference card: the directory's working set

Implementers print things. A one-page, print-optimized reference of
the most-used elements — those on the UNLK (the master document's
fields) — with tag, name, representation, and position.

## Tasks

- [x] `/reference` — a print-styled page: the UNLK-bridged elements
      grouped by form line, with tag / 1993-name-aware name /
      representation / position. One A4 page target.
- [x] @media print: the existing element-page print block extended
      or a dedicated print stylesheet for this page.
- [x] Contracts: /reference builds; contains UNLK-bridged elements;
      print media styles present.

## Beats

A printed instrument from the registry — the thing standards users
actually tape to their wall.
